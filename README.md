# HomeMascot

デスクトップマスコット的なやつを作りました。実質セーブ用です。

## Build

```bash
./gradlew build
```

## Configuration

- Weather API key is read from `local.properties` as `WEATHER_API_KEY` (not committed).

## License

MIT (see `LICENSE`).
